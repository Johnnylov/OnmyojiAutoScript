import 'package:flutter/material.dart';

Widget getTitle(BuildContext context, {String? routePath}) {
  return const Padding(
    padding: EdgeInsets.only(left: 12),
    child: Text('OASX / Solana', style: TextStyle(fontSize: 13)),
  );
}
