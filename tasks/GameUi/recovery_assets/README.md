The four `cache_cleanup_*` templates are cropped from the September 22
`oas2_1790013339788` error screenshot at the game's 1280 by 720 resolution.
Only the shared prompt, cache labels and Cancel text are retained; changing
cache sizes, selected checkboxes and account information are excluded.

The prompt and both labels must match together. Navigation then permits only
Cancel, before local or generic confirmation actions, with at most three
delivered clicks on an unchanged popup. No cache deletion is requested.
